



import {Scene1} from "./Scene1"

var config = {
    type: Phaser.AUTO,
    width: 800,
    height: 600,
    backgroundColor: "222",
    parent: 'phaser-example',
    scene: [Scene]
};

export var game = new Phaser.Game(config);


